# Discord Bot - Система заявок в семью

Discord бот для обработки заявок на вступление в семью с модальными окнами и полным логированием событий сервера.

## 🎯 Функционал

### 📋 Система заявок
- **Модальное окно** для подачи заявки с полями:
  - Имя и Фамилия
  - Номер паспорта
  - Чем будете полезны в семье
  - OOC Имя
- **Автоматическая нумерация** заявок
- **Изменение никнейма** на формат: "Имя Фамилия | OOC Имя"
- **Автоматическая выдача роли** заявителя при подаче заявки
- **Embed-сообщения** с информацией о заявке
- **Упоминание** подавшего заявку в канале
- **Автоматическое создание панели** заявок при запуске бота

### ✅ Команды модерации
- `l.accept <номер заявки>` - принять заявку
  - Отправляет уведомление в ЛС пользователю
  - Показывает, кто принял заявку
  - **Выдает роли принятого** и снимает роль заявителя
  
- `l.decline <номер заявки> <причина>` - отклонить заявку
  - Отправляет уведомление с причиной в ЛС пользователю
  - Показывает, кто отклонил и почему

### 📊 Логирование
Все события сервера логируются в указанный канал с **указанием того, кто выполнил действие**:
- Вход/выход участников
- Удаление сообщений (кто удалил + причина)
- Редактирование сообщений
- Баны/разбаны (кто забанил/разбанил + причина)
- Создание/удаление ролей и каналов (кто создал/удалил + причина)
- Изменение ролей участников (кто добавил/удалил роли + причина)
- Изменение никнеймов (кто изменил + причина)
- Голосовая активность
- Обработка заявок

## 📁 Структура проекта

```
Lark/
├── main.py                    # Точка входа в приложение
├── requirements.txt           # Зависимости проекта
├── .env                       # Конфигурация (не в Git)
├── .env.example              # Пример конфигурации
├── .gitignore                # Игнорируемые файлы
├── README.md                 # Документация
│
├── config/                   # Конфигурация
│   ├── __init__.py
│   └── settings.py          # Настройки бота и ID каналов
│
├── utils/                    # Утилиты
│   ├── __init__.py
│   ├── storage.py           # Работа с хранением заявок
│   ├── role_manager.py      # Управление ролями и никнеймами
│   └── logger.py            # Система логирования
│
├── models/                   # Модели Discord UI
│   ├── __init__.py
│   ├── application_modal.py # Модальное окно заявки
│   └── application_button.py# Кнопка подачи заявки
│
├── commands/                 # Команды бота
│   ├── __init__.py
│   └── application_commands.py # Команды работы с заявками
│
├── events/                   # События бота
│   ├── __init__.py
│   ├── bot_events.py        # События запуска/готовности
│   └── server_events.py     # События сервера для логов
│
└── data/                     # Данные (создается автоматически)
    └── applications.json     # Хранилище заявок
```

## 🚀 Установка

### 1. Установите зависимости
```bash
pip install -r requirements.txt
```

### 2. Настройте токен бота
1. Скопируйте файл [`.env.example`](.env.example:1) в `.env`:
```bash
cp .env.example .env
```

2. Откройте `.env` и вставьте токен вашего бота:
```
DISCORD_TOKEN=ваш_токен_бота
```

### 3. Получение токена бота
1. Перейдите на [Discord Developer Portal](https://discord.com/developers/applications)
2. Создайте новое приложение или выберите существующее
3. Перейдите в раздел "Bot"
4. Скопируйте токен (если не видите, нажмите "Reset Token")
5. Включите следующие **Privileged Gateway Intents**:
   - ✅ PRESENCE INTENT
   - ✅ SERVER MEMBERS INTENT
   - ✅ MESSAGE CONTENT INTENT

### 4. Добавьте бота на сервер
1. В Developer Portal перейдите в "OAuth2" → "URL Generator"
2. Выберите:
   - **Scopes**: `bot`, `applications.commands`
   - **Bot Permissions**: 
     - Administrator (или настройте права вручную)
3. Скопируйте сгенерированную ссылку и откройте в браузере
4. Выберите сервер и подтвердите

## ⚙️ Настройка каналов

В файле [`config/settings.py`](config/settings.py:8) измените ID каналов на ваши:

```python
# ID каналов
APPLICATION_CHANNEL_ID = 1448168154235666502  # Канал для заявок
LOG_CHANNEL_ID = 1448195966573346827          # Канал для логов
PANEL_CHANNEL_ID = 1448019782614909090        # Канал для панели заявок

# ID ролей
APPLICANT_ROLE_ID = 1448026283060760686       # Роль для подающих заявки
ACCEPTED_ROLE_1_ID = 1448026775899865240      # Первая роль принятых
ACCEPTED_ROLE_2_ID = 1448026398403985408      # Вторая роль принятых
```

### Как получить ID канала/роли:
1. Включите режим разработчика в Discord (Настройки → Расширенные → Режим разработчика)
2. ПКМ по каналу → Копировать ID
3. ПКМ по роли → Копировать ID

## 🎮 Запуск

```bash
python main.py
```

При запуске бот автоматически:
- Загрузит сохраненные заявки из [`data/applications.json`](data/applications.json:1)
- Очистит канал панели заявок
- Создаст новую панель с кнопкой подачи заявки
- Отправит лог о запуске в канал логов

## 📖 Использование

### Поток работы с заявками

1. **Присоединение к серверу**
   - Новый участник автоматически получает роль заявителя

2. **Подача заявки**
   - Участник нажимает кнопку "📝 Подать заявку в семью"
   - Заполняет модальное окно с данными
   - **Автоматически изменяется никнейм** на формат: "Имя Фамилия | OOC Имя"
   - **Автоматически выдается роль заявителя** (если не была)
   - Заявка отправляется в канал для рассмотрения

3. **Рассмотрение заявки**
   - Модераторы видят заявку в канале
   - Принимают или отклоняют командой

4. **Принятие заявки**
   - Участнику в ЛС приходит уведомление
   - **Выдаются роли принятого**
   - **Снимается роль заявителя**

5. **Отклонение заявки**
   - Участнику в ЛС приходит уведомление с причиной
   - Роли остаются без изменений

### Автоматическая панель заявок
Бот автоматически создает панель заявок при каждом запуске в канале, указанном в `PANEL_CHANNEL_ID`. Все старые сообщения в этом канале будут удалены.

### Ручное создание панели заявок
Если нужно создать панель в другом канале, используйте команду:
```
/application_panel
```

### Обработка заявок
После подачи заявки она появится в канале для заявок с номером. Используйте команды:

**Принять заявку:**
```
l.accept 1
```

**Отклонить заявку:**
```
l.decline 1 Недостаточно опыта
```

## 🗂️ Модули проекта

### [`config/settings.py`](config/settings.py:1)
Содержит все настройки бота:
- Токен Discord
- ID каналов (заявки, логи, панель)
- Путь к файлу хранения заявок

### [`utils/storage.py`](utils/storage.py:1)
Управление хранением заявок:
- Загрузка/сохранение в JSON
- Получение следующего ID заявки
- CRUD операции с заявками

### [`utils/logger.py`](utils/logger.py:1)
Система логирования всех событий в канал логов

### [`utils/role_manager.py`](utils/role_manager.py:1)
Управление ролями и никнеймами:
- Установка никнейма заявителя
- Выдача роли заявителя при подаче заявки
- Выдача ролей принятому при принятии заявки
- Снятие роли заявителя при принятии
- Настройка нового участника

### [`models/application_modal.py`](models/application_modal.py:1)
Модальное окно с полями для заполнения заявки

### [`models/application_button.py`](models/application_button.py:1)
Кнопка для открытия модального окна

### [`commands/application_commands.py`](commands/application_commands.py:1)
Команды для работы с заявками:
- `/application_panel` - создание панели
- `l.accept` - принятие заявки
- `l.decline` - отклонение заявки

### [`events/bot_events.py`](events/bot_events.py:1)
События бота:
- Запуск и готовность
- Автоматическая настройка панели заявок

### [`events/server_events.py`](events/server_events.py:1)
События сервера для логирования:
- Участники (вход/выход)
- Сообщения (удаление/редактирование)
- Роли и каналы
- Голосовые каналы

## 📦 Требования

- Python 3.8+
- discord.py 2.3.2+
- python-dotenv 1.0.0+

## 🔧 Расширение функционала

Модульная структура позволяет легко добавлять новые возможности:

1. **Новые команды** → создайте файл в `commands/`
2. **Новые события** → создайте файл в `events/`
3. **Новые UI элементы** → создайте файл в `models/`
4. **Новые утилиты** → создайте файл в `utils/`

Примеры расширений:
- Система голосования за заявки
- Автоматическая выдача ролей при принятии
- Статистика по заявкам
- Экспорт данных в Excel/CSV
- Интеграция с базой данных

## 🐛 Отладка

### Проверьте при возникновении проблем:
1. Правильность токена в `.env`
2. Включены ли все необходимые intents в Developer Portal
3. Есть ли у бота права администратора на сервере
4. Существуют ли указанные каналы по ID
5. Создана ли папка `data/` (создается автоматически)

### Логи
Бот выводит информацию в консоль:
- Запуск бота
- Загрузка заявок
- Синхронизация команд
- Создание панели заявок
- Ошибки

## 📄 Лицензия

MIT License - смотрите файл [LICENSE](LICENSE:1)

---

**Проект**: FOR DRAGON FAMQ  
**Версия**: 2.0.0 (Модульная структура)